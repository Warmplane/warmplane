{
  "name": "warmplane-ui",
  "version": "0.27.0",
  "type": "module",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "bun build ./src/main.ts --outfile=./dist/bundle.js --minify && bun run ./build.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
