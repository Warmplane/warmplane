{
  "name": "warmplane-control-deck",
  "version": "0.25.1",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "bun build ./src/main.ts --outfile=./dist/bundle.js --minify && bun run ./build.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
