{
  "name": "warmplane-ui",
  "version": "0.11.2",
  "private": true,
  "scripts": {
    "build": "bun build ./src/main.ts --outfile=./dist/bundle.js --minify"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
